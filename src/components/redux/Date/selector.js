export const selectCurrentYear = (state) => state.calendar.currentYear;
export const selectCurrentMonth = (state) => state.calendar.currentMonth;
export const selectNotes = (state) => state.calendar.notes;
